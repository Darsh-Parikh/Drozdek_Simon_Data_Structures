#include "listInfo.h"
#include "listHidden.h"

/*Sorts the list based on IDs, in ascending order.*/
int sortList() {
    return 1;
}

/*sorts the list, but only the len number of elements of starting element.*/
int sortListPart(List* start, int len) {
    if (!start) { ll_err("Sort", 1); return 0; }
    return 1;
}